# Binaries
/costlane
/fakeprovider
*.exe
*.dll
*.so
*.dylib
dist/
bin/

# Crash dumps. A bash on Windows writes one of these into the working
# directory when the msys layer faults, and `git add -A` swept one into a
# commit on 2026-09-11. It is noise in a repository and it is noise in a
# diff.
*.stackdump
core
core.*

# Test and coverage
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Secrets and credentials
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
*~
